import { StyleSheet, View, Text, Image } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const HomeScreen = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Feels like home, doesn't it?</Text>
        <Text>Sí, si se siente</Text>
      </View>
    </SafeAreaView>
  );
};

export default HomeScreen;
